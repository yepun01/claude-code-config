# Session & history (personal, large)
history.jsonl
sessions/
debug/
file-history/
shell-snapshots/
session-env/

# Cache & telemetry (generated)
cache/
paste-cache/
statsig/
telemetry/
stats-cache.json
mcp-needs-auth-cache.json

# Plugins (installed separately)
plugins/

# Projects (per-user session state)
projects/

# Runtime state
teams/
tasks/
plans/
tmp/
backups/
.claude/

# Personal overrides (not shared)
settings.local.json
.credentials.json

# Memory knowledge graph (generated by MCP)
memory/knowledge-graph.jsonl
mcp.json

# Settings backups (restore via git history instead)
settings.json.backup*

# Generated reports (regenerable, not source of truth)
state/MEMORY-DREAM-*.md
state/NOTES.jsonl
decisions/*.tests.txt

# Image cache (runtime)
image-cache/

# Viewer build output
viewer/dist/
viewer/node_modules/
